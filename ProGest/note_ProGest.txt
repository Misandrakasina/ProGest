    if request.method == 'GET':
        return render_template("add.html")
    elif request.method == 'POST':
        # TO.DO: fontion ajout dans la BD
        numero = request.form['numero']
        designation = request.form['designation']
        prix = float(request.form['prix'])
        quantite = int(request.form['quantite'])
        addNewMaterial(numero, designation, prix, quantite)
        materials = getAllMaterials()
        return render_template("home.html", materials=materials)
    else:
        return render_template("home.html")

def addNewMaterial(numero, designation, prix, quantite):


Tableau de interface projet:
    <table class="table table-striped table-hover">
      <thead>
        <tr>
          <th scope="col">Numero</th>
          <th scope="col">Designation</th>
          <th scope="col">P.U (Ar)</th>
          <th scope="col">Quantité</th>
        </tr>
      </thead>
      <tbody>
      {%for material in materials%}
        <tr>
          <th scope="row">{{material[0]}}</th>
          <td>{{material[1]}}</td>
          <td>{{material[2]}}</td>
          <td>{{material[3]}}</td>
          <td><a href="/edit" type="button" class="btn btn-success">Modifier</a></td>
          <td><a href="/delete" type="button" class="btn btn-danger">Supprimer</a></td>
        </tr>
      {%endfor%}
      </tbody>
    </table>
  

def addNewProject(Projets, Statut, Priorité, Equipe) :
    try:
        database = mysql.connector.connect(host="localhost", user="root", password="", database="progest")
        cursor = database.cursor()
        cursor.execute("INSERT INTO add_projet VALUES (%s,%s,%s,%s)", (Projets, Statut, Priorité, Equipe))
        database.commit()
        database.close()
        return True
    except:#erreur
        return False

def getAllProjects():
    database = mysql.connector.connect(host="localhost",user="root",password="",database="progest")
    cursor = database.cursor()
    cursor.execute("SELECT * FROM add_projet")
    nomProjet = cursor.fetchall()
    return nomProjet



<body>
    <h1> <center> Ajouter un produit </center> </h1>

<form method="POST" action="/add">
  <div class="mb-3">
    <label for="numero" class="form-label">Numero</label>
    <input type="text" class="form-control" id="numero" name="numero">
  </div>

  <div class="mb-3">
    <label for="designation" class="form-label">Désignation</label>
    <input type="text" class="form-control" id="designation" name="designation">
  </div>

    <div class="mb-3">
    <label for="prix" class="form-label">Prix</label>
    <input type="text" class="form-control" id="prix" name="prix">
  </div>

  <div class="mb-3">
    <label for="quantite" class="form-label">Quantité</label>
    <input type="text" class="form-control" id="quantite" name="quantite">
  </div>
  
  
  <button type="submit" class="btn btn-primary">Ajouter</button>
</form>

</body>


@app.route('/')
def index():
    materials = getAllMaterials()
    return render_template("home.html", materials=materials)


@app.route('/add', methods=['GET', 'POST'])
def add():
    if request.method == 'GET':
        return render_template("add.html")
    elif request.method == 'POST':
        # TO.DO: fontion ajout dans la BD
        numero = request.form['numero']
        designation = request.form['designation']
        prix = float(request.form['prix'])
        quantite = int(request.form['quantite'])
        addNewMaterial(numero, designation, prix, quantite)
        materials = getAllMaterials()
        return render_template("home.html", materials=materials)
    else:
        return render_template("home.html")




def getAllMaterials():
    database = mysql.connector.connect(
        host="localhost",
        user="root",
        password="",
        database="jsaispas"
    )
    cursor = database.cursor()
    cursor.execute("SELECT * FROM jsaispas")
    materials = cursor.fetchall()
    return materials

def addNewMaterial(numero, designation, prix, quantite):
    try:
        database = mysql.connector.connect(
            host="localhost", user="root", password="", database="jsaispas"
        )
        cursor = database.cursor()
        cursor.execute("INSERT INTO jsaispas VALUES(%s, %s,%s, %s)",
                       (numero, designation, prix, quantite))
        database.commit()
        database.close()
        return True
    except:
        return False




/* General Table Styles */
.table {
    width: 100%;
    border-collapse: collapse;
}

.table th, .table td {
    padding: 8px;
    text-align: left;
}

.table-striped tbody tr:nth-of-type(odd) {
    background-color: #f9f9f9;
}

.table-hover tbody tr:hover {
    background-color: #f1f1f1;
}

/* Status Styles */
.status {
    display: inline-block;
    padding: 0.2em 0.6em;
    border-radius: 1em;
    font-size: 0.85em;
    font-weight: bold;
    color: #fff;
    text-align: center;
}
/* Priority Styles */
.priorité {
    display: inline-block;
    padding: 0.2em 0.6em;
    border-radius: 1em;
    font-size: 0.85em;
    font-weight: bold;
    color: #fff;
    text-align: center;
}

.equipe {
    display: inline-block;
    padding: 0.2em 0.6em;
    border-radius: 1em;
    font-size: 0.85em;
    font-weight: bold;
    color: #fff;
    text-align: center;
}
/* Button Styles */
.btn-success {
    background-color: #4caf50;
    border: none;
    color: white;
    padding: 6px 12px;
    text-align: center;
    text-decoration: none;
    display: inline-block;
    font-size: 14px;
    border-radius: 4px;
    cursor: pointer;
}

.btn-success:hover {
    background-color: #45a049;
}
.ajoutPro {
    background-color: #03a9f4;
    border: none;
    color: white;
    padding: 10px 20px;
    text-align: center;
    text-decoration: none;
    display: inline-block;
    font-size: 16px;
    border-radius: 50%;
    cursor: pointer;
}

.ajoutPro:hover {
    background-color: #0288d1;
}




    if request.method == 'GET':
        return render_template("add.html")

